<UserControl x:Class="Radar.Views.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:local="clr-namespace:Radar.Views"
             mc:Ignorable="d" 
            d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border CornerRadius="8" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                    Padding="15" Margin="10" Grid.Row="0">
                <Grid Margin="10"  >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border CornerRadius="8" Background="#FFF3F3F3" BorderBrush="#E0E0E0" BorderThickness="1" 
                            Padding="15"  Grid.Column="0">

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="70"/>
                                <RowDefinition Height="5"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>


                            <TextBlock Text="Paramétrage" Foreground="Black" FontSize="22" FontWeight="SemiBold" Grid.Row="0" Margin="5 25 0 10" TextAlignment="Center"/>

                            <!--Menu Seprator-->
                            <Separator Margin="25" Background="#FF2B2B2B" Grid.Row="1" Height="0.8"/>

                            <!--Menu Buttons-->
                            <StackPanel Width="200" Grid.Row="2">
                                <Button  Command="{Binding ShowPathsCommand}" Tag="Chemain">
                                    <StackPanel Orientation="Horizontal">
                                        <icon:PackIconMaterial Kind="MapMarkerPath" />
                                        <TextBlock Text="Chemain"/>
                                    </StackPanel>
                                </Button>

                                <Button Command="{Binding ShowSuperviserCommand}" Tag="Supervision">
                                    <StackPanel Orientation="Horizontal">
                                        <icon:PackIconMaterial Kind="AccountSupervisor"/>
                                        <TextBlock Text="Supervision"/>
                                    </StackPanel>
                                </Button>
                                



                            </StackPanel>
                        </Grid>
                        
                    </Border>

                    <Border CornerRadius="8" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                            Padding="15"  Grid.Column="1">

                        <ContentControl Content="{Binding SettingNavigationService.CurrentView}" 
                            HorizontalAlignment="Stretch" 
                            VerticalAlignment="Stretch" 
                            Visibility="Visible"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

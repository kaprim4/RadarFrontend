<Window x:Class="Radar.Views.RadarFetchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Radar.Views"
        mc:Ignorable="d"
        Title="RadarFetchWindow" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True" Background="Transparent"
        xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <Border CornerRadius="30" Background="#EFF2F7" BorderBrush="Black" OpacityMask="Black">
        <Grid Height="200" HorizontalAlignment="Center" Width="300">
            <Grid.RowDefinitions>
                <RowDefinition Height="90" Name="Icon" ></RowDefinition>
                <RowDefinition Height="50" Name="Text" ></RowDefinition>
                <RowDefinition Height="*" Name="Actions" ></RowDefinition>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <icon:PackIconMaterial Kind="Alert" HorizontalAlignment="Center" VerticalAlignment="Center" Height="80" Width="80" Foreground="Orange"/>
            </Grid>
            <Grid Grid.Row="1">
                <TextBlock Text="Un Radar est connecté" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
            <Grid Grid.Row="2">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                    <Button Content="Annuler" Width="75" Margin="5,0,0,0" Command="{Binding CancelCommand}" Style="{StaticResource cancelButton}" Background="#FFCACACA"/>
                    <Button Content="Récuperer les données" Width="150" Margin="5,0,0,0" Command="{Binding FetchDataCommand}" Style="{StaticResource addButton}"  />
                </StackPanel>
            </Grid>

        </Grid>
    </Border>
</Window>

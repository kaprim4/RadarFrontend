<UserControl x:Class="Radar.Views.DataView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Radar.Views"
      xmlns:vm="clr-namespace:Radar.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
              MinHeight="100" MinWidth="100">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="120"/>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Border CornerRadius="8" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                    Padding="15" Margin="10" Grid.Row="0">
            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  Margin="10,0,0,0" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock FontSize="32" FontWeight="SemiBold" Foreground="#121518" HorizontalAlignment="Left" VerticalAlignment="Center"><Run Language="fr-fr" Text="Données"/></TextBlock>

                <Grid Width="270" Grid.Row="4" Grid.Column="1" HorizontalAlignment="Right" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="43" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Margin="15 0" VerticalAlignment="Center" Foreground="#B0B9C6" IsHitTestVisible="False"
                       Panel.ZIndex="1" Text="Search in Members ..."
                       Visibility="{Binding ElementName=textBoxFilter, Converter={StaticResource BooleanToVisibilityConverter}}" />

                    <TextBox Name="textBoxFilter" Text="{Binding SearchValue, UpdateSourceTrigger=PropertyChanged}" 
                     Style="{StaticResource filterTextBox}" 
                     KeyDown="TextBoxFilter_KeyDown"/>

                    <Button Style="{StaticResource addButton}" Command="{Binding SelectFilesCommand}" Grid.Column="1" Width="43" Height="32" HorizontalAlignment="Center" Margin="5,9,0,11" >
                        <StackPanel Orientation="Horizontal" Height="18" Width="11">
                            <icon:PackIconMaterial Kind="Plus" VerticalAlignment="Center" Margin="0 1 8 0" Width="11" Height="11"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Grid>
        </Border>
        <Border CornerRadius="8" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                    Margin="15 5" Padding="15" Grid.Row="1">
            <Grid x:Name="MainContent" >


                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition />
                </Grid.RowDefinitions>


                <!--Members Data Grid-->
                <DataGrid Grid.Row="0" RowStyle="{DynamicResource DataGridRowStyle1}" ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" CellStyle="{DynamicResource DataGridCellStyle1}" x:Name="devicesDataGrid" ItemsSource="{Binding dataDataGrid}" Style="{DynamicResource DataGridStyle1}" >
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="C" CanUserResize="False" ElementStyle="{StaticResource CheckBoxStyle1}">
                            <DataGridCheckBoxColumn.HeaderTemplate>
                                <DataTemplate>
                                    <CheckBox Style="{StaticResource CheckBoxStyle1}"/>
                                </DataTemplate>
                            </DataGridCheckBoxColumn.HeaderTemplate>
                        </DataGridCheckBoxColumn>
                        <DataGridTextColumn Header="Id" IsReadOnly="True" CanUserResize="False" Width="*" Binding="{Binding Id}"/>
                        <DataGridTextColumn Header="Réference" Binding="{Binding Reference}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Date de Création" Binding="{Binding CreatedDate}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Nombre des fichiers" Binding="{Binding NbFiles}" IsReadOnly="True" Width="*"/>

                        <DataGridTemplateColumn Header="Operations" IsReadOnly="True" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Style="{StaticResource gridEditButton}" Background="#FFCACACA" Foreground="Black" Command="{Binding DataContext.ViewCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                            <icon:PackIconMaterial Kind="Eye" Style="{StaticResource gridButtonIcon}"/>
                                        </Button>
                                        <Button Margin="5 0 0 0" Style="{StaticResource gridRemoveButton}" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                            <icon:PackIconMaterial Kind="DeleteOutline" Style="{StaticResource gridButtonIcon}"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!--Pagingation Section-->
                <Border Grid.Row="1"  HorizontalAlignment="Right" BorderBrush="#DEE4EC" BorderThickness="1" CornerRadius="5" Padding="5" Background="#ffffff" Margin="0 10 10 10" MinHeight="30" MaxHeight="30">
                    <StackPanel Orientation="Horizontal">
                        <!-- Previous Button -->
                        <Button Style="{StaticResource pagingButton}" Command="{Binding DataContext.GoToPreviousPage, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <icon:PackIconMaterial Kind="ChevronLeft" Style="{StaticResource pagingButtonIcon}"/>
                        </Button>

                        <!-- Page Numbers -->
                        <Rectangle Width="1" Fill="#DEE4EC" Margin="3 7"/>

                        <!-- Dynamic Page Buttons -->
                        <ItemsControl ItemsSource="{Binding Pages}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource pagingButton}" 
                                            Command="{Binding DataContext.GoToPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}">
                                        <TextBlock Text="{Binding}" />
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Ellipsis for overflow pages -->
                        <TextBlock Text="..." Foreground="#6C7682" VerticalAlignment="Center" Margin="10 0" FontSize="15" Visibility="{Binding ShowEllipsis, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <!-- Next Button -->
                        <Rectangle Width="1" Fill="#DEE4EC" Margin="3 7"/>
                        <Button Style="{StaticResource pagingButton}" Command="{Binding DataContext.GoToNextPage, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <icon:PackIconMaterial Kind="ChevronRight" Style="{StaticResource pagingButtonIcon}"/>
                        </Button>
                    </StackPanel>
                </Border>



            </Grid>
        </Border>
    </Grid>


</UserControl>

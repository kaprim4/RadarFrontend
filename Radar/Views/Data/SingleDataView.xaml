<UserControl x:Class="Radar.Views.SingleDataView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Radar.Views"
      xmlns:utility="clr-namespace:Radar.Utilities"
      xmlns:vm="clr-namespace:Radar.ViewModels"
      d:DataContext="{d:DesignInstance Type=vm:SingleDataViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <utility:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Border CornerRadius="8" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" 
                    Padding="15" Margin="15" HorizontalAlignment="Stretch">
        <Grid>
            <Grid x:Name="MainContent" Grid.Column="1" >


                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>


                <Grid Grid.Row="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"  Margin="10,0,0,0" >

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid>
                        <Button Style="{StaticResource addButton}" Command="{Binding ReturnCommand}" Grid.Column="2" Width="43" HorizontalAlignment="Left" Margin="2,9,0,11" >
                            <StackPanel Orientation="Horizontal" Height="18" Width="11">
                                <icon:PackIconMaterial Kind="ArrowLeft" VerticalAlignment="Center" Margin="0 1 8 0" Width="11" Height="11"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                    <Grid Height="auto" Grid.Column="1" HorizontalAlignment="Stretch" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="195*" />
                            <ColumnDefinition Width="32*"/>
                            <ColumnDefinition Width="50" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Margin="15,0,15,0" VerticalAlignment="Center" Foreground="#B0B9C6" IsHitTestVisible="False"
                           Panel.ZIndex="1" Text="Search in Members ..."
                           Visibility="{Binding ElementName=textBoxFilter, Converter={StaticResource BooleanToVisibilityConverter}}" Grid.ColumnSpan="2" />

                        <TextBox Name="textBoxFilter" Text="{Binding SearchValue, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource filterTextBox}" 
                         KeyDown="TextBoxFilter_KeyDown" Grid.ColumnSpan="2"/>

                        <Button Style="{StaticResource addButton}" Command="{Binding SelectFilesCommand}" Grid.Column="2" Width="42" HorizontalAlignment="Center" Margin="0,9,0,11" >
                            <StackPanel Orientation="Horizontal" Height="18" Width="11">
                                <icon:PackIconMaterial Kind="Plus" VerticalAlignment="Center" Margin="0 1 8 0" Width="11" Height="11"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource addButton}" Command="{Binding AddDocumentCommand}" Grid.Column="2" Width="43" HorizontalAlignment="Left" Margin="2,9,0,11" >
                            <StackPanel Orientation="Horizontal" Height="18" Width="11">
                                <icon:PackIconMaterial Kind="Plus" VerticalAlignment="Center" Margin="0 1 8 0" Width="11" Height="11"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </Grid>

                <Grid Width="820" Grid.Row="1" Margin="10 15" VerticalAlignment="Stretch" HorizontalAlignment="Left"  >

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0">
                        <TextBlock>
                            <Run Text="Numéro de Lot :"/>
                            <Run Text="{Binding CurrentLot.Reference}" FontWeight="Bold"/>
                        </TextBlock>
                    </Grid>
                    <Grid Grid.Column="1">
                        <TextBlock>
                            <Run Text="Date de Creation :"/>
                            <Run Text="{Binding CurrentLot.CreateDate}" FontWeight="Bold"/>
                        </TextBlock>
                    </Grid>

                </Grid>

                <!--Members Data Grid-->
                <DataGrid RowStyle="{DynamicResource DataGridRowStyle1}" ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" CellStyle="{DynamicResource DataGridCellStyle1}" x:Name="devicesDataGrid" ItemsSource="{Binding DocumentsGrid}" Style="{DynamicResource DataGridStyle1}" >
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="C" CanUserResize="False" Width="auto" ElementStyle="{StaticResource CheckBoxStyle1}">
                            <DataGridCheckBoxColumn.HeaderTemplate>
                                <DataTemplate>
                                    <CheckBox Style="{StaticResource CheckBoxStyle1}"/>
                                </DataTemplate>
                            </DataGridCheckBoxColumn.HeaderTemplate>
                        </DataGridCheckBoxColumn>
                        <DataGridTextColumn Header="Id" IsReadOnly="True" CanUserResize="False" Width="auto" Binding="{Binding Id}"/>
                        <DataGridTextColumn Header="Réference" Binding="{Binding Reference}" IsReadOnly="True" Width="auto"/>
                        <DataGridTextColumn Header="Nom de fichier" Binding="{Binding Name}" IsReadOnly="True" Width="*"/>

                        <DataGridTemplateColumn Header="Operations" IsReadOnly="True" Width="auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Style="{StaticResource gridEditButton}" Background="#FFCACACA" Foreground="Black" Command="{Binding DataContext.ViewDocumentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                            <icon:PackIconMaterial Kind="Eye" Style="{StaticResource gridButtonIcon}"/>
                                        </Button>
                                        <Button Margin="5 0 0 0" Style="{StaticResource gridRemoveButton}" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                                            <icon:PackIconMaterial Kind="DeleteOutline" Style="{StaticResource gridButtonIcon}"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!--Pagingation Section-->
                <Border Grid.Row="6" HorizontalAlignment="Right" BorderBrush="#DEE4EC" BorderThickness="1" CornerRadius="5" Padding="5" Background="#ffffff" Margin="0 10 10 10">
                    <StackPanel Orientation="Horizontal">
                        <!-- Previous Button -->
                        <Button Style="{StaticResource pagingButton}" Command="{Binding GoToPreviousPage}">
                            <icon:PackIconMaterial Kind="ChevronLeft" Style="{StaticResource pagingButtonIcon}"/>
                        </Button>

                        <!-- Page Numbers -->
                        <Rectangle Width="1" Fill="#DEE4EC" Margin="3 7"/>

                        <!-- Dynamic Page Buttons -->
                        <ItemsControl ItemsSource="{Binding Pages}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource pagingButton}" 
                                    Command="{Binding DataContext.GoToPageCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}">
                                        <TextBlock Text="{Binding}" />
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Ellipsis for overflow pages -->
                        <TextBlock Text="..." Foreground="#6C7682" VerticalAlignment="Center" Margin="10 0" FontSize="15" Visibility="{Binding ShowEllipsis, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <!-- Next Button -->
                        <Rectangle Width="1" Fill="#DEE4EC" Margin="3 7"/>
                        <Button Style="{StaticResource pagingButton}" Command="{Binding GoToNextPage}">
                            <icon:PackIconMaterial Kind="ChevronRight" Style="{StaticResource pagingButtonIcon}"/>
                        </Button>
                    </StackPanel>
                </Border>


            </Grid>


            <!-- Loading Overlay - Covers entire UserControl -->
            <Grid x:Name="LoadingOverlay"
              Panel.ZIndex="1000"
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">

                <!-- Semi-transparent background -->
                <Rectangle Fill="#80000000" 
                       Stretch="Fill"/>

                <!-- Loading indicator -->
                <Border Background="#CC333333" 
                    CornerRadius="6" 
                    Padding="20"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center">
                    <StackPanel>
                        <ProgressBar IsIndeterminate="True" 
                                Width="150" 
                                Height="2" 
                                Margin="0,0,0,10"/>
                        <TextBlock Text="Loading..." 
                             Foreground="White" 
                             HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
         
    </Border>


</UserControl>

<Window x:Class="Radar.Views.AddUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Radar.Views"
        mc:Ignorable="d"
        Title="AddUserWindow" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">
    <Border CornerRadius="30" Background="#EFF2F7" BorderBrush="Black" OpacityMask="Black">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
            <StackPanel>
                <TextBlock Text="FullName :" />
                <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" />
                
                <TextBlock Text="Username :" />
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
                
                <TextBlock Text="Email :" Margin="0,10,0,0"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Phone :" Margin="0,10,0,0"/>
                <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Password :" Margin="0,10,0,0"/>
                <PasswordBox Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged"/>
                

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="Cancel" Width="75" Margin="5,0,0,0" Command="{Binding CancelCommand}" Style="{StaticResource cancelButton}" />
                    <Button Content="Save" Width="75" Margin="5,0,0,0" Command="{Binding SaveCommand}" Style="{StaticResource addButton}" Background="#FFCACACA" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
